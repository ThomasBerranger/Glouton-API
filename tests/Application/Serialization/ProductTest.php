<?php

namespace App\Tests\Application\Serialization;

class ProductTest
{
    // todo: tester les données des groupes de serialization
    //
    //$data = json_encode([
    //'name' => 'Test Product',
    //'price' => 100, // Ce champ ne doit pas être défini
    //]);
    //
    //$product = $this->serializer->deserialize($data, Product::class, 'json', ['groups' => ['product:write']]);
    //
    //$this->assertNull($product->getPrice());
}
